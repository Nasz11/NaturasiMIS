[deploy]
startCommand = "php artisan migrate --force && php artisan db:seed --class=ProductVariantSeeder && php artisan serve --host=0.0.0.0 --port=8080"
healthcheckPath = "/"
healthcheckTimeout = 300